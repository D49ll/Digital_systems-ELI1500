#include <stdio.h>

int main()
{
    int x[10];
    long long y[10];
    float z[10];
    long double w[10];

    for (int i=0;i<10;i++)
    {
        printf("int[%d]     =%d\nlong long[%d]=%d\nfloat[%d]    =%d\nlong double[%d]=%d\n\n",i,&x[i],i,&y[i],i,&z[i],i,&w[i]);
    }
    return 0;
}
