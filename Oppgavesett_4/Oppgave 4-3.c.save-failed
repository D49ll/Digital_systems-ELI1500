#include <stdio.h>
#include <time.h>

#define SIZE 10

int main ()
{
    srand(time(NULL));

    int tilf[SIZE];
    int i;
    for (i=0;i<SIZE;i++)
    {
        printf("%d",(rand()%20)+1);
    }


    return 0;


}
